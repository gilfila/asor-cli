{
  "name": "asor-cli",
  "version": "0.2.0",
  "description": "Turn each Workday ASOR agent into its own CLI: pick agents from your tenant's Agent System of Record and generate a dedicated, dependency-free command for Slack, Teams, Claude Code, or anything with a shell.",
  "type": "module",
  "license": "MIT",
  "author": "Tony Gilfillan",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gilfila/asor-cli.git"
  },
  "homepage": "https://github.com/gilfila/asor-cli#readme",
  "bugs": "https://github.com/gilfila/asor-cli/issues",
  "keywords": [
    "workday",
    "asor",
    "agent-system-of-record",
    "a2a",
    "agents",
    "cli",
    "slack",
    "teams"
  ],
  "bin": {
    "asor": "dist/src/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist/src",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "test": "npm run clean && npm run build && node --test \"dist/test/**/*.test.js\"",
    "mock": "npm run build && node dist/mock/server.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
